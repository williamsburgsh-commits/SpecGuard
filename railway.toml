[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build -w @specguard/web"

[deploy]
startCommand = "npm run start -w @specguard/web"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
